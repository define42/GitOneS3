The compiled TypeScript UI is embedded from this directory.
Run `make ui` to build it locally; Docker builds it automatically.
Generated index.html and assets are intentionally not committed.
This file keeps standalone Go tests usable without a Node installation.
